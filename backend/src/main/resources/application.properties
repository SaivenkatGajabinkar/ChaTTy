# Server Port
server.port=8080

# Database Configuration
spring.datasource.url=jdbc:postgresql://ep-round-wind-adzvb2hz-pooler.c-2.us-east-1.aws.neon.tech/neondb?sslmode=require&options=endpoint%3Dep-round-wind-adzvb2hz-pooler
spring.datasource.username=${DB_USER:neondb_owner}
spring.datasource.password=${DB_PASSWORD:npg_KeROBkAg15CI}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# CORS Configuration
app.cors.allowed-origins=${CORS_ORIGIN:http://localhost:5173}

# JWT Secret Key (Must be at least 256 bits/32 characters)
chaty.app.jwtSecret=${JWT_SECRET:404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970}
chaty.app.jwtExpirationMs=${JWT_EXPIRATION_MS:86400000}

# Google Cloud Translation API Key (Placeholder for now)
google.cloud.translation.api-key=${GOOGLE_CLOUD_API_KEY:YOUR_GOOGLE_CLOUD_API_KEY}
